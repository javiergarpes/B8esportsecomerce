import { StyleSheet } from "react-native";

export default styles = StyleSheet.create({
    cardContainer: {
        marginHorizontal:30,
        marginVertical:10,
        padding:10,
        width:'85%',
        justifyContent:'center',
        alignItems:'center',
        borderRadius:8,
        
    },
    text:{
      fontSize:30,
    }
})